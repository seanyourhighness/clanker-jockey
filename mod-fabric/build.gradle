plugins {
    id "fabric-loom" version "${loom_version}"
}

version = project.mod_version
group = project.maven_group

base {
    archivesName = project.archives_base_name
}

repositories {
    // No extra repositories needed yet.
}

dependencies {
    // Minecraft + mappings + loader
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"

    // Fabric API
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"

    // ClankerJockey core library (plain Java, no Minecraft deps)
    implementation project(":core")
}

processResources {
    inputs.property "version", project.version

    filesMatching("fabric.mod.json") {
        expand "version": project.version
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
    withSourcesJar()
}

jar {
    // Bundle the plain-Java core classes directly into the mod jar (same shading
    // pattern as mod-forge). Without this the Fabric jar is missing core and the
    // mod fails at runtime with NoClassDefFoundError.
    from(project(":core").sourceSets.main.output)
}
